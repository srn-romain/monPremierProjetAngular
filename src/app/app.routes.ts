import { Routes } from '@angular/router';
import {ComposentRouteurComponent} from "./composent-routeur/composent-routeur.component";

export const routes: Routes=[
  {path:'login',component:ComposentRouteurComponent}
]
